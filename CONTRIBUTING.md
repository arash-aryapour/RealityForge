# 🤝 راهنمای مشارکت در RealityForge

خیلی خوشحال می‌شویم که می‌خواهید در RealityForge مشارکت کنید! این سند شامل راهنمایی‌های مشارکت است.

---

## 🐛 گزارش باگ

### قبل از گزارش کردن:
1. دستاویزات را بررسی کنید
2. Issues موجود را جستجو کنید
3. مطمئن شوید که باگ هنوز وجود دارد

### نحوه گزارش:
```markdown
**توضیح مختصر**
[توضیح خلاصه از مشکل]


**محیط**
- OS: [مثلاً Windows 10]
- Python: [نسخه Python]
- Version: [نسخه RealityForge]
```

---

## 💡 ارائه انتقادهای سازنده

1. **برای ویژگی‌های جدید:**
   - توضیح دهید چرا لازم است
   - نمونه استفاده بدهید
   - مزایا و معایب را بیان کنید

2. **برای بهبود:**
   - مشکل فعلی را شرح دهید
   - راه‌حل پیشنهادی را بیان کنید
   - تأثیرات احتمالی را بررسی کنید

---

## 🔀 درخواست Pull Request

### تهیه:
```bash
# Fork کنید
git clone https://github.com/arash-aryapour/RealityForge.git

# Branch جدید ایجاد کنید
git checkout -b feature/feature-name

# تغییرات را انجام دهید
# کد خود را بهبود دهید
# تست کنید

git add .
git commit -m "Add: توضیح تغییرات"
git push origin feature/feature-name
```

### الزامات PR:
- ✅ کد تمیز و قابل فهم
- ✅ کامنت‌های واضح
- ✅ بدون تغییرات غیر ضروری
- ✅ پیام commit واضح
- ✅ اگر ممکن است، تست شامل کنید

### شرح PR:
```markdown
## توضیح
[توضیح مختصر تغییرات]

## نوع تغییر
- [ ] Bug fix
- [ ] Feature
- [ ] Documentation
- [ ] Performance

## Related Issues
Fixes #(issue number)

## تست‌ها
[نحوه تست کردن]

## تصاویر/ویدیو
[اگر مربوطه]
```

---

## 🎨 استاندارد کدنویسی

### PEP 8 را دنبال کنید:
```python
# ✅ درست
def generate_configs(count: int = 50) -> List[Dict]:
    """تولید کانفیگ‌های Reality."""
    configs = []
    for i in range(count):
        config = create_config(i)
        configs.append(config)
    return configs

# ❌ نادرست
def GenerateConfigs(COUNT=50):
    configs=[]
    for i in range(COUNT):
        config=CreateConfig(i)
        configs.append(config)
    return configs
```

### نام‌گذاری:
- `function_name`: توابع
- `ClassName`: کلاس‌ها
- `CONSTANT_NAME`: ثابت‌ها
- `_private_method`: متدهای خصوصی

### کامنت‌ها:
```python
# کامنت برای مرحله مختلف
def process_data(data):
    """توضیح مختصر تابع.
    
    Args:
        data: توضیح پارامتر
        
    Returns:
        توضیح خروجی
    """
    # اول: پاک کردن داده‌ها
    cleaned = clean(data)
    
    # دوم: پردازش
    result = transform(cleaned)
    
    return result
```

---

## 🧪 تست‌ها

### اجرای تست‌ها:
```bash
# تمام تست‌ها
python -m pytest

# تست خاص
python -m pytest tests/test_generator.py

# با coverage
python -m pytest --cov=realityforge
```

### نوشتن تست:
```python
def test_generate_configs_count():
    """تست تعداد کانفیگ‌های تولیدی."""
    configs = generate_configs(10)
    assert len(configs) == 10

def test_config_structure():
    """تست ساختار کانفیگ."""
    configs = generate_configs(1)
    config = configs[0]
    assert 'uuid' in config
    assert 'server' in config
    assert config['port'] == 443
```

---

## 📝 سند‌های مهم

### اگر سند اصلاح می‌کنید:
- ✅ Markdown را بررسی کنید
- ✅ لینک‌ها را تست کنید
- ✅ متن را برای وضوح بررسی کنید
- ✅ نمونه‌ها را به‌روز کنید

### نوشتن مستندات خوب:
```markdown
# عنوان واضح

## مقدمه
[توضیح مختصر]

## نحوه استفاده
[مثال عملی]

## نکات مهم
- نکته 1
- نکته 2

## منابع
[لینک‌های مفید]
```

---

## 🔄 فرآیند بررسی

1. **تجزیه و تحلیل**: بررسی کد
2. **تست‌ها**: اجرای تست‌های خودکار
3. **نظر‌خواهی**: دریافت نظر از متخصصان
4. **تأیید**: تأیید و Merge

---

## 💬 ارتباط

- **GitHub Issues**: مسائل فنی
- **Discussions**: سؤالات و بحث
- **Email**: arash.aryapour@proton.me

---

## ✨ ستاره برای اولین‌بار مشارکت‌کننده

برای اولین بار که PR خود را ارسال کردید:
- 🎉 تبریک!
- ⭐ شما در فهرست مشارکت‌کنندگان خواهید بود
- 🙏 بسیار تشکر می‌کنیم

---

## 📚 منابع اضافی

- [Git Documentation](https://git-scm.com/doc)
- [GitHub Guides](https://guides.github.com/)
- [Conventional Commits](https://www.conventionalcommits.org/)
- [PEP 8 Style Guide](https://pep8.org/)

---

## 🙏 تشکر!

هر مشارکتی برای ما مهم است! 💙

حتی اگر فقط یک باگ یا تایپ را گزارش دهید، برای ما بسیار ارزشمند است.

**متشکریم که بخشی از ما هستید!**
